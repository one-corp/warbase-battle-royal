[
  {
    "file": "/Users/vaidik/Developer/WarBase /client/src/main.ts",
    "chunks": [
      {
        "AllowMultiple": false,
        "EndLine": 20,
        "ReplacementContent": "    SceneInstrumentation,\n    EngineInstrumentation,\n    SSAO2RenderingPipeline,\n    ScreenSpaceReflectionPostProcess\n} from '@babylonjs/core';",
        "StartLine": 15,
        "TargetContent": "    SceneInstrumentation,\n    EngineInstrumentation,\n    KeyboardEventTypes,\n    SSAO2RenderingPipeline,\n    ScreenSpaceReflectionPostProcess\n} from '@babylonjs/core';"
      }
    ],
    "action": "Fixing unused import"
  },
  {
    "file": "/Users/vaidik/Developer/WarBase /client/src/physics/WeaponSystem.ts",
    "chunks": [
      {
        "AllowMultiple": false,
        "EndLine": 372,
        "ReplacementContent": "        viewmodelRoot.getChildMeshes().forEach((m: any) => {\n            const name = m.name.toLowerCase();\n            // TEMP: Do not disable the built-in default gun for testing\n            /*\n            if (name.includes(\"rifle\") || name.includes(\"weapon\") || name.includes(\"gun\") || name === \"mesh\") {\n                m.setEnabled(false);\n                console.log(\"Disabled duplicate built-in viewmodel mesh:\", m.name);\n            } else {\n            */\n                m.alwaysSelectAsActiveMesh = true;\n                m.isPickable = false;\n            // }\n        });",
        "StartLine": 364,
        "TargetContent": "        viewmodelRoot.getChildMeshes().forEach((m: any) => {\n            const name = m.name.toLowerCase();\n            if (name.includes(\"rifle\") || name.includes(\"weapon\") || name.includes(\"gun\") || name === \"mesh\") {\n                m.setEnabled(false);\n                console.log(\"Disabled duplicate built-in viewmodel mesh:\", m.name);\n            } else {\n                m.alwaysSelectAsActiveMesh = true;\n                m.isPickable = false;\n            }\n        });"
      },
      {
        "AllowMultiple": false,
        "EndLine": 480,
        "ReplacementContent": "        akRoot.setEnabled(false); // TEMP: Disable custom weapon\n        pistolRoot.setEnabled(false); // TEMP: Disable custom weapon\n        \n        // Ensure default fallback if activeWeapon is missing\n        WeaponStateComponent.activeWeapon[eid] = 0; // 0 = AK47, 1 = Pistol",
        "StartLine": 477,
        "TargetContent": "        pistolRoot.setEnabled(false); // Only one visible at a time\n        \n        // Ensure default fallback if activeWeapon is missing\n        WeaponStateComponent.activeWeapon[eid] = 0; // 0 = AK47, 1 = Pistol"
      }
    ],
    "action": "Disabling custom weapons and restoring default gun"
  }
]